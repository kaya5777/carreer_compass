<nav class="bg-white shadow-sm border-b border-gray-200">
  <div class="container mx-auto px-6 max-w-7xl">
    <div class="flex justify-between items-center h-16">
      <div class="flex items-center space-x-8">
        <%= link_to root_path, class: "flex items-center space-x-2" do %>
          <span class="text-xl font-bold text-indigo-600">Career Compass</span>
        <% end %>
        <% if user_signed_in? %>
          <div class="hidden md:flex items-center space-x-4">
            <%= link_to "ダッシュボード", dashboard_path, class: "text-gray-600 hover:text-indigo-600 text-sm font-medium" %>
          </div>
        <% end %>
      </div>
      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <span class="text-sm text-gray-600 hidden md:inline"><%= current_user.display_name || current_user.email %></span>
          <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "text-sm text-gray-600 hover:text-indigo-600 font-medium" %>
        <% else %>
          <%= link_to "ログイン", new_user_session_path, class: "text-sm text-gray-600 hover:text-indigo-600 font-medium" %>
          <%= link_to "新規登録", new_user_registration_path, class: "bg-indigo-600 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-indigo-700" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
